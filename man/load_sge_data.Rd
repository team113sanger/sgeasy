% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/facades.R
\name{load_sge_data}
\alias{load_sge_data}
\title{Load SGE data from files}
\usage{
load_sge_data(
  count_files,
  metadata_file,
  annotation_file = NULL,
  count_file_pattern = "*lib_counts.tsv.gz",
  exclude_patterns = NULL,
  metadata_join_by = "sequencing_id",
  calculate_duration = TRUE
)
}
\arguments{
\item{count_files}{Character vector of paths to count TSV files, or a
directory path to search for count files.}

\item{metadata_file}{Path to the sample metadata TSV file.}

\item{annotation_file}{Path to the VEP annotation file. If NULL, will
attempt to extract from metadata \code{vep_anno} column.}

\item{count_file_pattern}{Glob pattern for finding count files if
\code{count_files} is a directory (default: "*lib_counts.tsv.gz").}

\item{exclude_patterns}{Character vector of patterns to exclude from
count file discovery (default: NULL).}

\item{metadata_join_by}{Column name in metadata to join with SAMPLE
column in counts (default: "sequencing_id").}

\item{calculate_duration}{Logical; whether to calculate duration from
condition column (default: TRUE).}
}
\value{
An \link{SGEData} object containing:
\describe{
\item{counts}{The raw count data frame (access via \verb{@counts})}
\item{metadata}{The sample metadata data frame (access via \verb{@metadata})}
\item{annotation}{The variant annotation data frame (access via \verb{@annotation})}
\item{complete_dataset}{Counts joined with metadata (access via \verb{@complete_dataset})}
}
}
\description{
Loads count files, sample metadata, and variant annotations. This is the
first stage of the SGE analysis workflow.
}
\examples{
\dontrun{
data <- load_sge_data(
  count_files = "inputs/",
  metadata_file = "metadata/screen_metadata.tsv",
  annotation_file = "annotations/vep_annotations.tsv"
)
}

}
