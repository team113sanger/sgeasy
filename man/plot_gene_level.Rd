% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{plot_gene_level}
\alias{plot_gene_level}
\title{Plot gene-level functional scores}
\usage{
plot_gene_level(
  data,
  gene_id,
  score_col,
  class_col,
  fdr_col,
  score_label = "Functional score",
  fdr_threshold = 0.01,
  colours = consequence_colours,
  shapes = classification_shapes,
  point_size = 1.5,
  facet_nrow = 1
)
}
\arguments{
\item{data}{A data frame from \code{\link[=prepare_gene_level_data]{prepare_gene_level_data()}}. Use
\verb{$gene_data} for raw scores or \verb{$combined} for reweighted scores.}

\item{gene_id}{Gene identifier for the plot title.}

\item{score_col}{Column name for the score to plot. For raw data use
e.g. "adj_lfc_continuous"; for reweighted use "combined_lfc".}

\item{class_col}{Column name for functional classification. For raw data
use e.g. "functional_classification_continuous"; for reweighted use
"functional_classification".}

\item{fdr_col}{Column name for FDR values. For raw data use
e.g. "FDR_continuous"; for reweighted use "FDR".}

\item{score_label}{Label for the y-axis (default: "Functional score").}

\item{fdr_threshold}{FDR threshold for alpha transparency (default: 0.01).}

\item{colours}{Named vector of colors for slim_consequence values
(default: \link{consequence_colours}).}

\item{shapes}{Named vector of shapes for functional_classification values
(default: \link{classification_shapes}).}

\item{point_size}{Size of points (default: 1.5).}

\item{facet_nrow}{Number of rows for exon faceting (default: 1).}
}
\value{
A ggplot object.
}
\description{
Creates a gene-level plot showing functional scores across all exons,
with variants colored by consequence type and shaped by functional
classification. Supports both raw and reweighted scores.
}
\examples{
\dontrun{
gene_results <- prepare_gene_level_data(processed_data)

# Plot raw scores
plot_gene_level(
  data = gene_results$gene_data,
  gene_id = "BRCA1",
  score_col = "adj_lfc_continuous",
  class_col = "functional_classification_continuous",
  fdr_col = "FDR_continuous",
  score_label = "Adjusted LFC"
)

# Plot reweighted scores
plot_gene_level(
  data = gene_results$combined,
  gene_id = "BRCA1",
  score_col = "combined_lfc",
  class_col = "functional_classification",
  fdr_col = "FDR",
  score_label = "Weighted LFC"
)
}

}
\seealso{
\code{\link[=prepare_gene_level_data]{prepare_gene_level_data()}}
}
