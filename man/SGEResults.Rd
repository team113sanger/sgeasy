% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes.R
\name{SGEResults}
\alias{SGEResults}
\title{SGEResults Class}
\usage{
SGEResults(
  results = (function (.data = list(), row.names = NULL) 
 {
     if (is.null(row.names))
    {
         list2DF(.data)
     }
     else {
         out <- list2DF(.data,
    length(row.names))
         attr(out, "row.names") <- row.names
         out
     }

    })(),
  rlog = NULL,
  contrast_summary = (function (.data = list(), row.names = NULL) 
 {
     if
    (is.null(row.names)) {
         list2DF(.data)
     }
     else {
         out <-
    list2DF(.data, length(row.names))
         attr(out, "row.names") <- row.names
      
      out
     }
 })(),
  metadata = list()
)
}
\description{
An S7 class that holds results from SGE differential abundance analysis.
}
\details{
Access properties using the \code{@} operator:
\itemize{
\item \code{results@results} - DESeq2 results table
\item \code{results@rlog} - Regularized log-transformed data
\item \code{results@contrast_summary} - Combined contrast statistics
\item \code{results@metadata} - Analysis parameters and timestamp
}
}
\section{Slots}{

\describe{
\item{\code{results}}{A data frame (tibble) containing DESeq2 results with a
SEQUENCE column identifying each variant.}

\item{\code{rlog}}{A DESeqTransform object containing regularized log-transformed
counts for visualization.}

\item{\code{contrast_summary}}{A data frame (tibble) containing combined summary
statistics across all contrasts, including log2 fold changes, p-values,
z-scores, and optional rate estimates.}

\item{\code{metadata}}{A list of analysis metadata including condition levels,
shrinkage type, alpha threshold, and analysis timestamp.}
}}

\examples{
\dontrun{
# Run analysis (returns SGEResults object)
results <- run_differential_analysis(counts, norm_counts, metadata)

# Print shows summary
results

# Access properties
results@results
results@contrast_summary
results@rlog

# Get rlog matrix
SummarizedExperiment::assay(results@rlog)

# Check analysis metadata
results@metadata$analysis_date
}

}
