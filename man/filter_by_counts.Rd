% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-wrangling.R
\name{filter_by_counts}
\alias{filter_by_counts}
\title{Filter variants by count thresholds}
\usage{
filter_by_counts(
  data,
  min_counts = 10,
  max_counts = Inf,
  exclude_pattern = "_0_",
  sample_pattern = "count"
)
}
\arguments{
\item{data}{A data frame with count data.}

\item{min_counts}{Minimum total count to retain (default: 10).}

\item{max_counts}{Maximum total count to retain (default: Inf).}

\item{exclude_pattern}{Pattern for columns to exclude from output
(default: "\emph{0}"). Set to NULL to keep all columns.}

\item{sample_pattern}{Pattern to identify sample columns for
count summation (default: "count").}
}
\value{
Filtered data frame with variants meeting count criteria.
}
\description{
Removes variants with total counts outside specified bounds and
optionally excludes specific sample patterns.
}
\details{
This function calculates the total count for each variant across all
sample columns matching \code{sample_pattern} using \code{starts_with()}, then
filters to retain only variants within the specified count range.
Optionally removes columns matching \code{exclude_pattern} (e.g., day 0 samples).
}
\examples{
\dontrun{
filtered_data <- filter_by_counts(
  data = count_data,
  min_counts = 10,
  exclude_pattern = "_0_"
)
}

}
