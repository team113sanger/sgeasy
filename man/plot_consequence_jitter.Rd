% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{plot_consequence_jitter}
\alias{plot_consequence_jitter}
\title{Plot jittered strip chart by group}
\usage{
plot_consequence_jitter(
  data,
  score_col,
  group_col,
  consequences = default_consequences,
  fdr_col = NULL,
  fdr_threshold = 0.01,
  colours = consequence_colours,
  xlab = group_col,
  ylab = score_col,
  show_median = TRUE,
  point_size = 1,
  jitter_width = 0.3
)
}
\arguments{
\item{data}{A data frame containing the score, grouping, and
optionally FDR columns.}

\item{score_col}{Character. Name of the column containing the numeric
scores to plot on the y-axis.}

\item{group_col}{Character. Name of the column containing the grouping
variable (e.g., consequence type) for the x-axis.}

\item{consequences}{Character vector. Only include rows where the
\code{group_col} value is in this vector. The factor levels of the
grouping variable will be set to this order. Set to \code{NULL} to
include all groups without filtering
(default: \link{default_consequences}).}

\item{fdr_col}{Character or NULL. Name of the column containing FDR
values. When provided, points with FDR >= \code{fdr_threshold} are
rendered semi-transparent.}

\item{fdr_threshold}{Numeric. FDR cutoff for full opacity
(default: 0.01).}

\item{colours}{Named character vector of point colours keyed by group
values (default: \link{consequence_colours}).}

\item{xlab}{Character. X-axis label (default: value of \code{group_col}).}

\item{ylab}{Character. Y-axis label (default: value of \code{score_col}).}

\item{show_median}{Logical. Whether to draw a horizontal median line
per group (default: TRUE).}

\item{point_size}{Size of jittered points (default: 1).}

\item{jitter_width}{Width of horizontal jitter (default: 0.3).}
}
\value{
A ggplot object.
}
\description{
Creates a jitter (strip) plot showing individual data points grouped by
a categorical variable, with optional median lines and FDR-based
transparency. Inspired by Figure 1E of Olvera-Leon et al. (2024) Cell.
}
\examples{
\dontrun{
# Basic usage
plot_consequence_jitter(
  data = results,
  score_col = "adj_score_condition_Day14_vs_Day4",
  group_col = "slim_consequence"
)

# With FDR-based transparency and selected consequences
plot_consequence_jitter(
  data = results,
  score_col = "adj_score_condition_Day14_vs_Day4",
  group_col = "slim_consequence",
  fdr_col = "FDR_condition_Day14_vs_Day4",
  consequences = c("synonymous", "missense", "stop_gained", "frameshift"),
  ylab = "z-score (D4-D14)"
)
}

}
