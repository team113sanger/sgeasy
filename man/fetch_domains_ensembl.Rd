% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/domains.R
\name{fetch_domains_ensembl}
\alias{fetch_domains_ensembl}
\title{Fetch protein domains from Ensembl REST API}
\usage{
fetch_domains_ensembl(
  transcript_id,
  species = "human",
  domain_sources = NULL,
  timeout = 30
)
}
\arguments{
\item{transcript_id}{Character. Ensembl transcript ID (e.g., "ENST00000355451").}

\item{species}{Character. Species name (default: "human"). Currently unused
but reserved for future multi-species support.}

\item{domain_sources}{Character vector. Filter to specific domain sources
(e.g., c("Pfam", "SMART")). If NULL (default), returns all sources.}

\item{timeout}{Numeric. Request timeout in seconds (default: 30).}
}
\value{
A tibble with columns: domain, source, start, end.
Returns NULL if no domains are found.
}
\description{
Retrieves protein domain annotations for a given transcript from the
Ensembl REST API.
}
\details{
The function first looks up the protein ID associated with the transcript,
then fetches all protein features annotated as domains.
}
\examples{
\dontrun{
domains <- fetch_domains_ensembl("ENST00000355451")
domains <- fetch_domains_ensembl("ENST00000355451",
                                  domain_sources = c("Pfam", "SMART"))
}

}
